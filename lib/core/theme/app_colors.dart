import 'package:flutter/material.dart';

abstract final class AppColors {
  // ── Light theme ──
  static const cream = Color(0xFFFFF8F0);
  static const warmWhite = Color(0xFFFFFCF8);
  static const espresso = Color(0xFF3C2415);
  static const espressoLight = Color(0xFF5C3D2E);
  static const espressoMuted = Color(0xFF8B7355);
  static const sage = Color(0xFF8FA68A);
  static const sageMuted = Color(0xFFB5C9B0);
  static const sageLight = Color(0xFFDFECDB);
  static const terracotta = Color(0xFFCC704B);
  static const terracottaLight = Color(0xFFE8956E);
  static const terracottaMuted = Color(0xFFF5C4A8);

  // ── Dark theme (warm dark) ──
  static const darkBg = Color(0xFF1A1410);
  static const darkSurface = Color(0xFF2A2118);
  static const darkCard = Color(0xFF352A20);
  static const darkBorder = Color(0xFF4A3D30);
  static const darkTextPrimary = Color(0xFFF5E6D3);
  static const darkTextSecondary = Color(0xFFB8A899);

  // ── Semantic ──
  static const error = Color(0xFFD64045);
  static const errorLight = Color(0xFFFDE8E8);
  static const success = Color(0xFF4CAF50);
  static const successLight = Color(0xFFE8F5E9);
  static const warning = Color(0xFFF5A623);
  static const warningLight = Color(0xFFFFF3E0);

  // ── Star rating ──
  static const starFilled = Color(0xFFE8956E);
  static const starEmpty = Color(0xFFD9CFC5);
}
